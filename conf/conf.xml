 <?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE devices PUBLIC "-//YARP//DTD yarprobotinterface 3.0//EN" "http://www.yarp.it/DTD/yarprobotinterfaceV3.0.dtd">

     <device xmlns:xi="http://www.w3.org/2001/XInclude" name="yarpLoggerRerun" type="yarpLoggerRerun">
        <param name="axesNames">(neck_pitch, neck_roll, neck_yaw, l_shoulder_pitch, l_shoulder_roll, l_shoulder_yaw, l_elbow, l_wrist_yaw, l_wrist_roll, l_wrist_pitch, r_shoulder_pitch, r_shoulder_roll, r_shoulder_yaw, r_elbow, r_wrist_yaw, r_wrist_roll, r_wrist_pitch, 
            torso_roll, torso_pitch, torso_yaw, r_hip_pitch, r_hip_roll, r_hip_yaw, r_knee, r_ankle_pitch, r_ankle_roll, l_hip_pitch, l_hip_roll, l_hip_yaw, l_knee, l_ankle_pitch, l_ankle_roll,
            l_thumb_add, l_thumb_prox, l_thumb_dist, l_index_add, l_index_prox, l_index_dist, l_middle_prox, l_middle_dist, l_ring_prox, l_ring_dist, l_pinkie_prox, l_pinkie_dist,
            r_thumb_add, r_thumb_prox, r_thumb_dist, r_index_add, r_index_prox, r_index_dist, r_middle_prox, r_middle_dist, r_ring_prox, r_ring_dist, r_pinkie_prox, r_pinkie_dist)</param>
        <param name="logIEncoders">true</param>
        <param name="logIMotorEncoders">false</param>
        <param name="logIPidControl">false</param> <!-- WARNING: rpc calls, so they might slow down the logging thread -->
        <param name="logITorqueControl">false</param> 
        <param name="logIAmplifierControl">false</param>
        <param name="logIControlMode">false</param>
        <param name="logIInteractionMode">false</param>
        <param name="logIMotorTemperatures">false</param>
        <param name="logILocalization2D">false</param>
        <param name="logIRawValuesPublisher">false</param>
        <param name="logURDF">true</param>
        <param name="fileName">log_test</param>
        <param name="filePath">/home/mgloria/iit/yarp-device-rerun/</param>
        <param name="saveToFile">false</param>
        <param name="viewerIp">localhost</param>

         <action phase="startup" level="5" type="attach">
             <paramlist name="networks">
                <!-- motorcontrol -->
                <elem name="head">head_hardware_device</elem>
                <elem name="left_arm_joints1"> left_arm_hardware_device </elem>
                <elem name="right_arm_joints1"> right_arm_hardware_device </elem>
                <elem name="torso"> torso_hardware_device </elem>
                <elem name="left_leg_joints"> left_leg_hardware_device </elem>
                <elem name="right_leg_joints"> right_leg_hardware_device </elem>
                <elem name="left_arm_joints2"> left_hand-coupling_handler </elem>
                <elem name="right_arm_joints2"> right_hand-coupling_handler </elem>
             </paramlist>
         </action>
         <action phase="shutdown" level="15" type="detach" />

     </device>